{
  "name": "crypto-trader-bot",
  "version": "1.0.0",
  "description": "## Project Structure",
  "main": "index.ts",
  "scripts": {
    "prettier": "prettier --write .",
    "lint": "npm run lint --workspace=crypto-trader-console && npm run lint --workspace=server && npm run lint --workspace=crypto-trader-db && npm run lint --workspace=trader-bot",
    "prepare": "husky || exit 0",
    "build:packages": "npm run build --workspace=crypto-trader-db",
    "build": "npm run build:packages && npm run build --workspace=server && npm run build --workspace=crypto-trader-console"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "prettier": "^3.6.2"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "private": true,
  "workspaces": [
    "crypto-trader-console",
    "trader-bot",
    "server",
    "packages/*"
  ]
}
